# ZOL网页结构分析与爬虫问题诊断

## 分析的URL
1. 产品列表页: https://detail.zol.com.cn/gpswatch/apple/
2. 产品详情页: https://detail.zol.com.cn/GPSwatch/index2141422.shtml
3. 产品评论页: https://detail.zol.com.cn/2142/2141422/review.shtml
4. 产品图片页: https://detail.zol.com.cn/2142/2141422/pic.shtml

---

## 爬虫代码问题诊断

### ❌ 问题1：URL格式提取错误
**代码位置**：zol_wristband_scraper.py:172
```python
# 当前代码
match = re.search(r'index(\d+)\.shtml', base_url)
# 预期格式: index2141422.shtml
# 实际URL: https://detail.zol.com.cn/GPSwatch/index2141422.shtml
```

**问题**：`index` 和数字之间缺少分隔符匹配

---

### ❌ 问题2：详情页URL构造错误
**代码位置**：zol_wristband_scraper.py:210, 226
```python
param_url = f"{base_path}/{product_id}/param.shtml"
pic_url = f"{base_path}/{product_id}/pic.shtml"
review_url = f"https://detail.zol.com.cn/GPSwatch/{product_id}/review.shtml"
```

**实际URL格式**：
- 参数页：`/2142/2141422/param.shtml`
- 图片页：`/2142/2141422/pic.shtml`
- 评论页：`/2142/2141422/review.shtml`

**问题**：
1. 缺少类别ID (2142)
2. 只有一个产品ID，需要两个ID

---

### ❌ 问题3：评论数据结构不匹配
**代码位置**：zol_wristband_scraper.py:264-266
```python
for item in soup.select('.review-item, .comment-item'):
    rating_elem = item.select_one('.rating, .score')
    content_elem = item.select_one('.content, .comment-content')
```

**实际评论页结构**（从web_fetch结果）：
- 评论是纯文本段落
- 没有username字段
- 没有rating字段（只有评论内容）
- 没有`.review-item`或`.comment-item`结构

---

### ❌ 问题4：图片选择器可能无效
**代码位置**：zol_wristband_scraper.py:232
```python
for img in soup_pic.select('.pic-list img, .gallery img, img[src*="product"]')[:2]:
```

**实际图片页结构**（从web_fetch结果）：
- 图片直接在页面中，没有嵌套的容器
- 图片URL格式：`https://2f.zol-img.com.cn/product/273_280x210/913/ceib1jzrSrUZk.jpg`
- 有11张图片

---

### ❌ 问题5：产品列表页选择器需要验证
**代码位置**：zol_wristband_scraper.py:120
```python
for pic in soup.select('.pic'):
```

**需要验证**：
- `.pic` 类是否存在于产品列表页
- 价格提取逻辑是否正确

---

## 建议的修复方案

### 1. 修复URL提取
```python
# 正确的URL格式提取
match = re.search(r'/(\d+)/index(\d+)\.shtml', base_url)
if match:
    category_id = match.group(1)  # 2142
    product_id = match.group(2)   # 2141422
```

### 2. 修复页面URL构造
```python
param_url = f"https://detail.zol.com.cn/{category_id}/{product_id}/param.shtml"
pic_url = f"https://detail.zol.com.cn/{category_id}/{product_id}/pic.shtml"
review_url = f"https://detail.zol.com.cn/{category_id}/{product_id}/review.shtml"
```

### 3. 修复评论抓取
```python
# 评论页结构是纯文本段落，需要重新分析
reviews = []
comment_sections = soup.select('.comment-section, .review-content')  # 需要实际页面结构
for section in comment_sections:
    comment_text = section.get_text(strip=True)
    if comment_text and comment_text != '[无用户评论]':
        reviews.append({
            'rating': '',
            'comment': comment_text[:1000]
        })
```

### 4. 修复图片抓取
```python
# 直接查找所有img标签，按属性筛选
images = []
for img in soup.find_all('img'):
    src = img.get('src', '')
    alt = img.get('alt', '')
    # 筛选产品图片（排除icon、广告等）
    if 'product' in src or ('外观图' in alt or len(src) > 50):
        full_url = src if src.startswith('http') else f"https:{src}"
        images.append(full_url)
```

---

## 需要进一步确认的信息

1. **产品列表页的实际HTML结构** - 需要查看`.pic`元素的具体结构
2. **评论页的正确选择器** - 需要分析评论的包裹元素
3. **价格信息的提取规则** - 需要确认价格字段在列表页的位置